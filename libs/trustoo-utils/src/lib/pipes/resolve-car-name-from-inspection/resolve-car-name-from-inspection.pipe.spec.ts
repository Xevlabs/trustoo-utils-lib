import { ResolveCarNameFromInspectionPipe } from './resolve-car-name-from-inspection.pipe';

describe('ResolveCarNameFromInspectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveCarNameFromInspectionPipe();
    expect(pipe).toBeTruthy();
  });
});
