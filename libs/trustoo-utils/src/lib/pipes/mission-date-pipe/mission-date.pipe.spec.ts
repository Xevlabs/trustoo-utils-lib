import { MissionDatePipe } from './mission-date.pipe';

describe('MissionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MissionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
